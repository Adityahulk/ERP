-- Placeholder: pricing_mode was added as 047 directly on server (never committed).
-- Renamed to 048 here to resolve the duplicate version conflict.
-- No schema changes needed; pricing_mode feature was not implemented.
SELECT 1;
